---
"eve": patch
---

Add durable attachment references for trusted Host Runtime applications. User and tool files can now remain in the host store and resolve only for each model request without persisting file bytes in eve history or sandbox state.
