---
"eve": patch
---

Add trusted host runtime snapshots for server-owned model, instruction, tool, timeout, and specialist configuration. Opaque references now stay durable through root and delegated execution, with replay-safe acceptance and terminal release notifications.
