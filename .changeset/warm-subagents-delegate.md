---
"eve": patch
---

`subagent.called` events now include the exact delegation message for local, remote, dynamic, continued, and Workflow-dispatched subagent calls, allowing clients to attribute parent and child work without parsing synthesized prompts.
