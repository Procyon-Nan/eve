---
"eve": patch
---

`subagent.called` events now include the exact delegation message for local, remote, dynamic, and continued subagent calls, allowing clients to attribute parent and child work without parsing synthesized prompts.
