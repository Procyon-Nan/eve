---
"eve": patch
---

Subagent child streams now include stable parent session, turn, call, and subagent name metadata in `session.started`, allowing clients to correlate parent and child streams safely.
